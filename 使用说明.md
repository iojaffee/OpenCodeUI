# OpenCodeUI Standalone 一键部署使用说明

## 前置条件

- 已安装 `opencode` CLI 工具
- 已安装 Docker 和 Docker Compose
- 项目已克隆到本地

## 快速使用

```bash
# 进入项目目录
cd OpenCodeUI

# 赋予脚本权限（首次执行一次）
chmod +x start.sh stop.sh

# 一键启动（会自动启动 opencode serve + 前端容器）
./start.sh

# 一键停止
./stop.sh
```

## 启动后的信息

脚本启动成功后，会在终端输出类似以下内容：

```
============================================
         ✅ 全部服务启动成功！
============================================
🌐 前端访问地址: http://localhost:27483
🔗 后端地址:     http://localhost:4096
📄 前端端口:     27483
📄 停止服务:     ./stop.sh
============================================
```

用浏览器打开显示的前端地址即可使用。

## 手动操作参考

### 单独启动 opencode serve

```bash
# 后台启动，监听所有接口（必须，否则容器连不上）
nohup opencode serve --host 0.0.0.0 > /tmp/opencode-serve.log 2>&1 &

# 查看日志
cat /tmp/opencode-serve.log

# 停止 serve
pkill -f "opencode serve"
```

### 单独启动前端容器

```bash
# 使用环境变量覆盖端口和后端地址
BACKEND_URL=host.docker.internal:4096 PORT=8080 \
  docker compose -f docker-compose.standalone.yml up -d

# 停止
docker compose -f docker-compose.standalone.yml down
```

### 查看运行状态

```bash
# 查看 Docker 容器状态
docker compose -f docker-compose.standalone.yml ps

# 查看 serve 进程
ps aux | grep opencode

# 查看端口占用
ss -tlnp | grep -E '4096|PORT'
```

## 常见问题

### Q: 前端页面显示连不上后端

A: 确认 `opencode serve` 是否启动，且监听在 `0.0.0.0:4096`（不是 `127.0.0.1`）：

```bash
ss -tlnp | grep 4096
```

如果不是，先停止 serve，重新用 `--host 0.0.0.0` 启动。

### Q: 端口被占用

A: `start.sh` 会自动搜索空闲端口，无需手动处理。如果想指定端口，修改 `start.sh` 中的 `PORT_RANGE_START` 和 `PORT_RANGE_END` 即可。

### Q: 如何修改后端地址

A: 编辑 `start.sh`，修改 `BACKEND_URL=host.docker.internal:4096` 中的地址即可。

## 文件说明

| 文件 | 说明 |
|------|------|
| `start.sh` | 一键启动脚本（serve + 前端） |
| `stop.sh` | 一键停止脚本 |
| `docker-compose.standalone.yml` | Docker Compose 配置文件 |
| `serve.log` | opencode serve 日志（`/tmp/opencode-serve.log`） |